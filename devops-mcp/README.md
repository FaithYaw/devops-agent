## Run locally

```bash
uv run python server.py
```

The server listens on `http://localhost:9090/mcp` by default.

## Run with Docker

From the repository root:

```bash
docker compose up --build mcp-server
```
