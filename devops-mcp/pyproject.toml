[project]
name = "devops-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexayaw", email = "coconutxinxin@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "docker>=7.2.0",
    "httpx>=0.28.1",
    "mcp>=2.1.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
devops-mcp = "devops_mcp:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
